public class Pythagorean {
    public double calculateHypotenuse(int legA, int legB) {
        // the hypotenuse is the side across from the right angle.
        // calculate the value of a c given legA and legB

        double squareRootA = Math.sqrt(legA);
        double squareRootB = Math.sqrt(legB);
        double hypotenuse = (legA + legB);
        return hypotenuse;

        double sqrtC = Math.sqrt(legA) + Math

    }
}